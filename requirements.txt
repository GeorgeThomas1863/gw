# All packages available in Anaconda 2023 (no pip install needed in that environment)
# Listed here for documentation purposes.
#
# Standard library (no install required):
#   tkinter, sqlite3, re, json, logging, datetime, os, sys, time,
#   webbrowser, threading, pathlib, unittest.mock, argparse
#
# Anaconda 2023 standard packages (pre-installed):
requests>=2.28.0
pytest>=7.0.0
